# yeah-med-be

backend for medication management

## docker

- commands
  - START daemon
    ```bash
      colima start
    ```
  - BUILD
    ```bash
      docker-compose up --build -d
    ```
  - LOGS
    ```bash
    docker-compose logs -f
    ```
  - DOWN
    ```bash
    docker-compose down
    ```
